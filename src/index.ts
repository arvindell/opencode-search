import { Plugin } from "@opencode/plugin";

// The session picker runs in the local CLI, including when connected to a remote server.
export default Plugin.define({ id: "opencode-search", setup() {} });
